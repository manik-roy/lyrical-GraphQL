/* eslint-disable react/display-name */
import React from 'react';

// eslint-disable-next-line react/prop-types
export default ({ children }) => (
  <div className="container">
    <h2 className="center">Lyrical Music</h2>
    {children}
  </div>
);
